function UI ({addFunc, delFunc}) {

    const addFuncHand = () => {
        const input = document.createElement('input');
        input.setAttribute('placeholder')
    }

    let num = 0;

    document.getElementById('inputButton').addEventListener('click', addFuncHand);


}